#title


#Generate Data frame
```{r}
df <- data.frame(x=rnorm(100), y=rnorm(100))
```
#Plot
```{r}
ggplot(df, aes(x,y)) +
  geom_point() +
  stat_smooth(method= lm, formula= y~x, se=FALSE) +
  theme_classic()
```

